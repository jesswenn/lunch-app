import React, { Component } from 'react';

const Spinner = (props) => { 
    return (
        <div className="spinner-container">
            <h2>SPINNER HERE</h2>
        </div>
    );
}

export default Spinner;
