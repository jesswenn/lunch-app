import React, { Component } from 'react';

import Button from '../Button/Button';
const Home = (props) => { 
    return (
        <div className="btn-container">
            <Button />
            <Button />
            <Button />
        </div>
    );
}

export default Home;
